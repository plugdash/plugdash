{
	"name": "sharepost",
	"tagline": "Share buttons without 200KB of JavaScript",
	"wordpressEquivalent": "Social Warfare / AddToAny (basic tier)",
	"npmPackage": "@plugdash/sharepost",
	"capabilities": "read:content, write:content",
	"hooks": "content:afterSave",
	"status": "beta",
	"installCommand": "npm install @plugdash/sharepost",
	"configExample": "import { defineConfig } from \"astro/config\";\nimport emdash from \"emdash\";\nimport { sharepostPlugin } from \"@plugdash/sharepost\";\n\nexport default defineConfig({\n\tintegrations: [\n\t\temdash({\n\t\t\tplugins: [sharepostPlugin()],\n\t\t}),\n\t],\n});",
	"pairs": "shortlink, heartpost",
	"highlights": [
		"Generates sharing URLs for Twitter/X, LinkedIn, WhatsApp, Bluesky, and email on publish",
		"ShareButtons.astro companion with four variants (circle, pill, ghost, filled)",
		"Zero client-side JavaScript - share links are plain anchor tags",
		"Inline SVG icons for zero external requests",
		"Optional Twitter via handle and hashtags"
	]
}
