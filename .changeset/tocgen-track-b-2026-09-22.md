---
"@plugdash/tocgen": patch
---

Fix compatibility with emdash 0.13.0+ (removed `definePlugin()` wrapper for sandboxed plugins) and migrate to canonical capability names (`content:read`, `content:write`). Adds test coverage for the existing admin settings page.
