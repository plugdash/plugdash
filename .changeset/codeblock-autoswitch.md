---
"@plugdash/codeblock": minor
---

Switch between github-dark and github-light by default when no theme is set, fix light mode leaving dark text on white span patches, and stop site pre styles (borders, background images) bleeding into blocks
